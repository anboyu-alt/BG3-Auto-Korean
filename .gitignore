# Python
__pycache__/
*.py[cod]
*.pyo

# PyInstaller
build/
dist/
*.spec.bak

# Nuitka / 빌드 산출물 (커밋 금지)
nuitka_build/
nuitka_onefile/
nuitka_*_log.txt
nuitka_*.log
BG3_ModTranslator/
BG3_ModTranslator.dist/
BG3_ModTranslator.build/
*.zip
*_setup.exe
pyi_*.log
_locacheck/

# 번역 캐시 (지우면 재번역 비용 발생 — 절대 삭제 금지!)
translation_cache.json

# 번역 로그
translation_errors.txt

# 임시 폴더
_pak_temp/

# 진단 산출물 (커밋 금지)
_diag2/
repair_report_*.json
repair_report_*.md

# 설정 (개인 API 키 포함)
# %APPDATA%\BG3-Auto-Korean\config.json 은 추적하지 않음

# Claude Code
.claude/

# Visual brainstorming session files
.superpowers/

# 테스트 파일
*-test.py

# pytest 임시 산출물 (tmp_path 등 — 커밋 금지)
pytest-of-*/
.pytest_cache/

# 개인 환경용 에이전트 지침 (context-mode 라우팅 등 — repo 비포함)
/CLAUDE.md
